server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    #serviceUrl:
      #defaultZone: http://discovery-server:8761/eureka
      
management:
  endpoints:
    web:
      exposure:
        include:
          - health
  tracing:
    sampling:
      probability: 1.0
      
